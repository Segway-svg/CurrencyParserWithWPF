<Window x:Class="WpfApp1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1"
        mc:Ignorable="d"
        Title="ValueParser" Height="450" Width="800">
    <Grid>
        <DataGrid x:Name="ValueParserList" FontSize="20" Margin="10" AutoGenerateColumns="False" Loaded="ValueParserList_Loaded">
            <DataGrid.Columns>
                <DataGridTextColumn Binding="{Binding Path = charCode}" Header="Currency" Width="100"/>
                <DataGridTextColumn Binding="{Binding Path = value}" Header="Value" Width="100"/>
            </DataGrid.Columns>    
        </DataGrid>
    </Grid>
</Window>
